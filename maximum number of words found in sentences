class Solution:
    def mostWordsFound(self, sentences: List[str]) -> int:
        max_word = 0

        for sentence in sentences:
            word_count = len(sentence.split())

            if max_word<word_count:
                max_word = word_count
        return max_word
