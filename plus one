class Solution:
    def plusOne(self, digits: List[int]) -> List[int]:
        ans = ""

        for i in digits:
            ans+=str(i)

        ans = str(int(ans)+1)

        res = []

        for i in ans:
            res.append(int(i))
        return res
