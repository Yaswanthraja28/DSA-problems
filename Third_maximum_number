class Solution:
    def thirdMax(self, nums: List[int]) -> int:
        unique_num = list(set(nums))

        unique_num.sort(reverse = True)

        if len(nums)>=3:
            return unique_num[2]
        return unique_num[0]third
