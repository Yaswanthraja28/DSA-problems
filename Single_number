def singlenumber(nums):
    map_freq = {}
    
    for num in nums:
        map_freq[num] = map_freq.get(num,0)+1
        
    for num in map_freq:
        if map_freq[num]==1:
            return num
        
nums = [4,1,2,1,2]
print(singlenumber(nums))
