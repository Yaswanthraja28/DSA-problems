def isAnagram(s, t):
    sorted_s = sorted(s)
    sorted_t = sorted(t)
    if sorted_s == sorted_t:
        return True
    else:
        return False

if __name__ == "__main__":
    s = "cat"
    t = "bat"

    print(True if isAnagram(s, t) else False)
