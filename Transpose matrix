class Solution:
    def transpose(self, matrix: List[List[int]]) -> List[List[int]]:
        ans = []

        for i in range(len(matrix[0])):
            count = []
            for j in range(len(matrix)):
                count.append(matrix[j][i])

            ans.append(count)

        return ans
